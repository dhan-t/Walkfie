package com.example.walkfie;

public enum StoryType {
    YOUR_STORY, // Represents the current user's story circle (for adding/viewing their own)
    FRIEND_STORY // Represents a friend's story circle (for viewing their stories)
}